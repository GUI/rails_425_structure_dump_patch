require 'spec_helper'

describe Rails425StructureDumpPatch do
  it 'has a version number' do
    expect(Rails425StructureDumpPatch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
